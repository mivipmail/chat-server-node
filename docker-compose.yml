version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
#    environment:
#      VIRTUAL_HOST: localhost
    restart: always
    container_name: chat-server_app

